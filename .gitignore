# Raw downloads and intermediate tables: several GB, regenerated by the pipeline.
# The four submitted deliverables live directly in data/ and ARE tracked.
data/raw/
data/interim/

# Superseded duplicates parked in bin/ (the small drafts there stay tracked).
bin/*.tsv
bin/*.txt
bin/*.png

# Python / IDE / OS
__pycache__/
.idea/
.DS_Store
